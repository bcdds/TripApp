// Paste your Google Sheets "Published as CSV" URLs here.
// File -> Share -> Publish to web -> Select each sheet -> CSV -> Copy link.

export const CONFIG = {
  RATES_CSV: 'sample/Rates.csv',       // replace with your published CSV
  ITINERARY_CSV: 'sample/Itinerary.csv',
  LODGINGS_CSV: 'sample/Lodgings.csv',
  EXPENSES_CSV: 'sample/Expenses.csv',
}
